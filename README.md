# API - Jogo Caca ao Tesouro

API em Python com FastAPI para consultar partidas e casas do banco SQLite.

## Executar

```bash
pip install -r api/requirements.txt
uvicorn api.main:app --reload
```

Documentacao interativa: `/docs`.
Especificacao OpenAPI: `/openapi.json`.
